{
  "buildCommand": "cd .. && pnpm build:shared && pnpm --filter @sme-insightx/client build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
